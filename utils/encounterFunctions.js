// encDateTime: DateTime string in ISO ISO 8601 extended format (YYYY-MM-DDTHH:mm:ss.sss±HH:mm) adapted to utc 0
// provider:    Array of the unique provider
// obs: List of observations
export function fillEncounterDB(patientUUID, encDateTime, locationUUID, encTypeUUID, providers){
    if(formString === undefined) return "";
    if(formString === null) return "";
    return formString.replace(/'/g, SQUOTE);
};